import React, { useState } from "react";

const Review = () => {
  const [img, setImg] = useState(null);
  return (
    <div>
      <form>
        <label></label>
      </form>
    </div>
  );
};

export default Review;
